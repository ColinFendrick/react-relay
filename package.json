{
  "name": "colin-react-relay",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.5",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "axios": "^0.21.0",
    "eslint": "^7.12.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-relay": "^10.0.1",
    "react-scripts": "4.0.0",
    "relay-runtime": "^10.0.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "npm run relay && react-scripts start",
    "build": "npm run relay && react-scripts build",
    "relay": "./node_modules/.bin/relay-compiler --schema schema.graphql --src ./src/ --watchman false $@",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "relay-compiler": "./node_modules/.bin/relay-compiler"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "babel-plugin-relay": "^10.0.1",
    "graphql": "^15.3.0",
    "relay-compiler": "^10.0.1"
  }
}
